# Core dependencies
openai>=1.0.0
langchain>=0.1.0
langchain-core>=0.1.0
langgraph>=0.1.0
langmem>=0.1.0

# Data processing
pandas>=1.5.0
numpy>=1.24.0
requests>=2.28.0

# Vector storage and search
faiss-cpu>=1.7.0

# PDF processing (requires external GROBID service)
# xml parsing is included in Python standard library

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0
